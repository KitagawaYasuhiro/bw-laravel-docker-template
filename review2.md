# Laravel Lesson レビュー②

## Todo編集機能

### @method('PUT')を記述した行に何が出力されているか
hidden属性のinputタグが生成され、これはHTTP　PUTメソッドを実行する。
<input type="hidden" name="_method" value="PUT">

### findメソッドの引数に指定しているIDは何のIDか
ルートパラメータで指定した$idを受け取り、
DBのIDカラムが$idと一致しているレコードを取得する。

### findメソッドで実行しているSQLは何か
SELECT文を実行し、指定されたIDのレコードを1件だけ取得する処理。
SELECT * FROM todos WHERE id = {$id} LIMIT 1;

### findメソッドで取得できる値は何か
IDカラムが一致しているtodoモデルのインスタンス。

### saveメソッドは何を基準にINSERTとUPDATEを切り替えているのか
モデルのexistsプロパティがfalseの場合INSERT文を、tureの場合UPDATE文を実行する。

## Todo論理削除

### traitとclassの違いとは
クラスの継承とは異なり、1つのクラスに複数のトレイトを追加することができる。
また、トレイト自体はインスタンス化できない。

### traitを使用するメリットとは
クラスにプロパティやメソッドを追加することができ、
複数のクラス間でコードを共通化し、再利用することができる。

## その他

### TodoControllerクラスのコンストラクタはどのタイミングで実行されるか
ルート指定されたコントローラーのメソッドが実行された時点でコンストラクタが実行される。

### RequestクラスからFormRequestクラスに変更した理由
FormRequestクラスはバリデーションを行うクラスの基本となるクラスである。
このクラスにバリデーションルールの設定を行うため。

### $errorsのhasメソッドの引数・返り値は何か
has()メソッドの引数には入力欄のname属性を記述する。
返り値として、バリデーションエラーがある場合にtrue、ない場合にfalseを返す。

### $errorsのfirstメソッドの引数・返り値は何か
first()メソッドの引数には入力欄のname属性を記述する。
返り値として、バリデーションエラーが発生した場合の最初のエラーメッセージをstring型で返す。
エラーが存在しない場合はNULLを返す。

### フレームワークとは何か
フレームワークとは、ルーティングやDB操作などをあらかじめ組み込んだプログラムの雛形である。
Laravelのようなフレームワークではディレクトリ構成があらかじめ決められており、
指定のディレクトリにファイルを配置したり、処理を書き換えていくことで効率的にアプリケーションを作成できる。

### MVCはどういったアーキテクチャか
Model, View, Controllerの略で、Routeを起点にリクエストに応じた処理をControllerを通じて、
Modelを介しDB操作やViewに処理結果を渡して画面の生成などを行う。

### ORMとは何か、またLaravelが使用しているORMは何か
Object-Relational Mappingの略。
ORMとはプログラミング言語のClassとDBのテーブルをマッピングすることで、
SQLを直接操作することなくClassのメソッドを用いることでDBとのやり取りを行うことができるものである。
LaravelではEloquent ORMが使用されている。

### composer.json, composer.lockとは何か
composer.jsonは、プロジェクトが依存するパッケージやライブラリの情報を記述する設定ファイルである。
ここでどのパッケージ（バージョン）を使うかなどを定義する。
composer.lockは、composer.jsonに基づき実際にインストールされたパッケージの正確なバージョンと依存関係を記録するファイルである。
composer install実行時にこのファイルを参照して、完全に同じバージョンを再現する。

### composerでインストールしたパッケージ（ライブラリ）はどのディレクトリに格納されるのか
vendor/に格納される。これはcomposer install実行時に自動生成される。