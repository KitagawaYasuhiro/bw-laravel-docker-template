# Laravel Lesson レビュー①

## Todo一覧機能

### Todoモデルのallメソッドで実行しているSQLは何か
SELECT * FROM todos;
all() メソッドは、todosテーブルの全レコードを取得するためのSELECT文を発行する。

### Todoモデルのallメソッドの返り値は何か
Todoモデルの all() メソッドの返り値は、Illuminate\Database\Eloquent\Collectionクラスのインスタンス。
todosテーブルの全レコードを取得し、それらをEloquentモデルのコレクションとして返す。

### 配列の代わりにCollectionクラスを使用するメリットは
Collectionインスタンスは配列操作に特化したクラスで、豊富なメソッドが使用でき、メソッドチェーンにより簡潔で直感的な操作が可能となる。

### view関数の第1・第2引数の指定と何をしているか
第一引数には表示したいBladeファイルを指定し、第二引数には渡したいデータを連想配列の形で渡すことができる。
view('todo.index', ['todos' => $todos])
と記述することでビュー内で$todosという変数が使えるようになる。

### index.blade.phpの$todos・$todoに代入されているものは何か
$todosには$todo->all()の結果が代入されており、これはtodosテーブルの全レコードを取得したEloquentモデルのコレクションとなる。
 $todoは、$todosの中から1件ずつ取り出した個々のToDoオブジェクト。
@foreach ($todos as $todo)
$todoはApp\Todoモデルのインスタンスのため、$todo->contentなどのプロパティが使える。

## Todo作成機能

### Requestクラスのallメソッドは何をしているか
リクエストで送信されたすべての入力データを連想配列として取得するためのメソッド。
フォームから送信された値を個別ではなく一括で取得している。

### fillメソッドは何をしているか
モデルに対して複数の属性を一括で代入するためのメソッド。
連想配列で所得した値をTodoインスタンスの各プロパティに一括で代入している。

### $fillableは何のために設定しているか
ユーザーが意図的にidなどを送信するとセキュリティ上の問題が発生する可能性があるため。

### saveメソッドで実行しているSQLは何か
新規作成の場合INSERT文が実行され、既存の場合はUPDATE文が実行される。
Modelクラスのexistsプロパティを使ってsave() 実行時にINSERT（新規）かUPDATE（既存）を判断する。

### redirect()->route()は何をしているか
指定した名前付きルートにリダイレクトしている。
return redirect()->route('todo.index');
todo.indexという名前のルートを探し、そのルートのURLを取得しリダイレクトする。

## その他

### テーブル構成をマイグレーションファイルで管理するメリット
データベース構造のバージョン管理ができ、ロールバックや再実行が簡単に行える。
また、チーム開発での同期が簡単に行える。

### マイグレーションファイルのup()、down()は何のコマンドを実行した時に呼び出されるのか
php artisan migrate　が実行されたときup()が呼び出され、
php artisan migrate:rollback　が実行されるとdown()が呼び出される。

### Seederクラスの役割は何か
データベースに初期データやテストデータを投入するための仕組み。

### route関数の引数・返り値・使用するメリット
名前付きルートからURLを生成し、保守性の高いコードを書くことができる。

### @extends・@section・@yieldの関係性とbladeを分割するメリット
- @extends('layouts.base')は親テンプレートを指定する構文で、子ビューがlayouts/base.blade.phpを継承することを意味する。
- @yield('content')は親テンプレート側に記述し、子ビューから差し込まれる場所を定義する。
- @section('content')は子ビュー側に記述し、@yield('content')に差し込む内容を定義する。

メリットとして、共通レイアウトを再利用することで保守性が高まり、コードの可読性も向上する。

### @csrfは何のための記述か
Bladeテンプレートで CSRF攻撃を防ぐためのトークンをフォームに埋め込むための記述。

### {{ }}とは何の省略系か
PHPの echo 文の省略記法で、HTMLエスケープ処理を行うe()関数が使われている。